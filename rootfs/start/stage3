# Passed from stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# . /start/stage2.functions
# isFirstRun
# VAR_*
# ---------------------------------------------------------

if [ "$isFirstRun" == "true" ]
then
   VAR_PGPASSFILE="$(makePwFileForUser "$VAR_USER")"
   /bin/chown $VAR_LINUX_USER "$VAR_PGPASSFILE"
fi
